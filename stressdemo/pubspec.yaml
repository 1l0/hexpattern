name: stressdemo
description: "Public Key Color Stress Demo"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  hexpattern:
    path: ../

  cupertino_icons: ^1.0.8
  string_validator: ^1.1.0
  statescope: ^2.0.1
  nostr_core_dart: ^1.0.1
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
